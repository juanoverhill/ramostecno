export class Equipo {
    constructor(public id: any, public descripcion: string, public marca_id: string) {
        this.id = id;
        this.descripcion = descripcion;
        this.marca_id = marca_id;
    }
}
